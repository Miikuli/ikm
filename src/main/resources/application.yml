spring:
  application:
    name: ikmtemplate

  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Vetv3434
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080

springdoc:
  swagger-ui:
    path: /docs